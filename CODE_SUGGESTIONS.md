# Suggestions de code (actions rapides)

- `anonymize.py`: ajouter un mode seed pour sorties deterministes.
- `audit-ad.ps1`: ajouter un parametre `--Format` (CSV/JSON).
- `phishing_score.py`: ajouter un detail des mots trouves.
- `fim.py`: ajouter un rapport JSON des changements.
- `password_gen.py`: ajouter un mode `--copy` (presse-papiers).
